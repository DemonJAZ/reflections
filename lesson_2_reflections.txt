What happens when you initialize a repository? Why do you need to do it?

It creates a hidden folder named .git , and enables version-control in that directory.

How is the staging area different from the working directory and the repository? What value do you think it offers?

Staging area prepares the files to be committed , its like putting chocolates in a box and then sending it out.

How can you use the staging area to make sure you have one commit per logical change?

simply using git diff command.

What are some situations when branches would be helpful in keeping your history organized? How would branches help?

it can be useful to try experimental things without effecting the master branch and disturbing other users of the repoitory.

How do the diagrams help you visualize the branch structure?

it lets us know which commits are reachable from particular commit.

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?

This combines the addition to codes of multiple user branched to a single project. Diagram shows the the attachment of the branch
to the master branch making the project development process more clear.

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?

Manual merging can be discussed and taken care of with the person who has also caused changes 
